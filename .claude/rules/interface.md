---
paths:
  - "src/components/**"
  - "src/app/**/*.tsx"
  - "src/styles/**"
---

Nenhum componente calcula número: indicadores saem de `src/server/metrics/queries.ts`. Procure
componente existente antes de criar. Cor de texto usa a variante `-tx`; tamanho, trilha e raio só
da escala fechada (`npm run design` reprova valor cru). `:hover` e media query vivem em
`theme.css`. Erro mostra erro, vazio explica, zero ≠ indisponível. Detalhes: skills
`ui-ux-system` e `design-matrix`.
